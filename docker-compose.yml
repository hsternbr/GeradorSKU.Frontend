version: "3.9"

services:
  autodeploy:
    build:
      context: .
    container_name: ${CONTAINER_NAME}
    ports:
      - "4021:4021"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - TZ=America/Sao_Paulo
    restart: unless-stopped
